@model IEnumerable<UserModel>
@{
    ViewData["Title"] = "Test List Model";
}

<h1>Test pass list of model from controller to view</h1>
<hr />

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.firstname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.lastname)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.firstname
                </td>
                <td>
                    @item.lastname
                </td>
            </tr>
        }
    </tbody>
</table>
